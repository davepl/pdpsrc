# UNIX-Gary on both PDPs — September 23, 2026

Public URL: <https://pdp1173.com/pdp-ai.html>. The page title is UNIX-Gary;
the assistant's name is PDP Gary. The latest page includes the deep-green
frame, green portrait, Apple touch icon and Open Graph preview metadata.
The decorative cursor beside READY FOR INPUT has been removed.

## Deployment

The HTML and four web images were copied to `/home/www` on `.26`, matching
the existing `.29` deployment byte for byte. Images were uploaded and verified
before the page, using temporary files and atomic renames with mode 644.
The `.26` backup/deployment manifest is on caddy at
`/var/backups/pdp-ai/20260923T153620Z-pdp26`.
The previous `.29` HTML, before adding Apple preview metadata, is on caddy at
`/var/backups/pdp-ai/20260923T153223Z-page/pdp-ai.html`.

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| `pdp-ai.html` | 27947 | `6b73e1e177add5b817ad3f1e488e7a61e279b65ef4b80936e04b5e1ff92206f8` |
| `gary-green-v1.jpg` | 43347 | `4d0f4a5067c1a99c8360d003c1cc1313aee5ac7041b6c253b878fd24a7644365` |
| `unix-gary-apple-touch-icon-v2.png` | 42927 | `59f87186a4c4cc806dfec5b43bf95cb5275f056587d079e8981ba111276e13f5` |
| `unix-gary-favicon-v2.png` | 2595 | `24a8981c081488d8a0c9a7713c407f143eaa6f98b0790e2a0ee8c9000b7bd493` |
| `unix-gary-preview-v1.jpg` | 136684 | `d58f3a5b53722c0ca7580ccd158e3e7b5b818b52a7de9c79960808ae23701303` |

The AI bridge on ubvmdell was updated to allow the `.26` LAN origin in addition
to `.29` and the public hostnames. The model service and personality are
unchanged. Credentials remain private on ubvmdell and caddy; Git includes only
the route template and credential paths.
The previous bridge is backed up at
`/home/dave/.local/share/pdp-ai/backups/20260923T154123Z/server.py` on ubvmdell.

Caddy now corrects the legacy HTTP server's text/plain MIME type for the two
PNG icon paths to image/png. The page uses v2 icon URLs so caches cannot reuse
the earlier text/plain responses. Configuration validation, reload, and direct
proxy response checks passed. The previous route is backed up at
`/var/backups/pdp-ai/20260923T154235Z-icons/pdp-ai-route.caddy` on caddy.
The follow-up route that adds v2 icon URLs has its previous copy at
`/var/backups/pdp-ai/20260923T154513Z-icons/pdp-ai-route.caddy`.
The final versioned-icon HTML was verified on both PDPs; the prior primary
HTML is preserved at `/var/backups/pdp-ai/20260923T154619Z-page/pdp-ai.html`.
The original pre-update `.26` HTML remains in the first deployment backup above.

## Live failover verification

The active/saved VCL stayed `pdp_auto_20260922T182449Z_fee419`, in `auto` mode,
with `.29` primary and `.26` fallback. Neither PDP was stopped or restarted.
Holding the backend administration lock, a serial check fetched each of the
five files through Caddy with the public Host header and a temporary cookie
to bypass Varnish's cached copy:

1. Both healthy: every file reported `X-PDP-Node: PDP .29` and `X-Cache: PASS`.
2. The active VCL's `.pdp29` admin health was briefly set to `sick`: every file
   reported `.26`, still with exact matching bytes.
3. A `finally` handler restored `.pdp29` admin health to `auto`: all five
   files again reported `.29` and matched byte for byte.

Both origins ended with probe health 3/3 healthy. The saved VCL remained
unchanged. Cached static pages still follow the same five-minute freshness
and grace policy as the homepage; the check intentionally bypassed that cache
to verify actual origin selection. Public HTTPS also returned the current
HTML and all preview/icon assets byte for byte.

The six isolated bridge tests passed with both PDP origins and the public
origin. The isolated real-Varnish suite passed with chat-page/asset checks
added for primary, probe-detected fallback, recovery, and foreground retries.
A public chat request bearing the `.26` Origin streamed a complete response
in which the assistant identified itself as PDP Gary; its preflight response
allowed the same origin.

## Source preservation

[PR #10](https://github.com/davepl/pdpsrc/pull/10) is based on the existing automatic-failover/shared-counter branch
(PR #8), so this change does not duplicate or replace that infrastructure.
The static page, images, high-resolution artwork, AI bridge/persona, user
service, Caddy route template, tests and restoration instructions are under
`bsd/www`. Native installation and source bundles include the new page/assets.
