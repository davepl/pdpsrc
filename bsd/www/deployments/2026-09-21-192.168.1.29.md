# Complete application deployed to 192.168.1.29

Deployment date: September 21, 2026. Source application: 192.168.1.26.

The target previously served the older static homepage. It is running 2.11BSD
patch 498, kernel MENTEC #9, with the existing `www` account (uid 80, gid 10),
`/home/www` document root, and HTTP launched through inetd. Its HTTP server
binary matched the preserved original server exactly and was retained.

## Installation

- Confirmed the live `.26` homepage, `webtop.c`, `webtop-cgi.c`,
  `visit-counter.c`, and Makefile matched this repository.
- Archived the original `.29` website, server binary, inetd configuration,
  services file, and startup file before installation; copied the archive
  off the PDP.
- Uploaded the complete source bundle to `/usr/src/local/webtop-source.tar`
  and extracted it into `/usr/src/local/webtop`. Staging used `/usr` because
  the approximately 3.9 MiB bundle would nearly fill the root filesystem.
- Ran `make clean`, `make`, and `make install` on the target. Both build and
  installation completed successfully. The sampler was compiled with the
  target's headers and separate instruction/data space.
- Installed the red-and-gold page, webtop, visitor counter, photo, and linked
  TMOG banner. No counter existed on `.29`, so it received an independent
  total initialized at zero; `.26`'s count was not copied or changed.
- Changed only the inetd invocation in `/etc/rc` to
  `/usr/sbin/inetd -R 1000`, following `config/README.inetd.md`. Checked shell
  syntax, retained root ownership/mode 644, and restarted inetd with that rate.
  The original startup file is `/etc/rc.before-webtop-20260921`.
- The public proxy configuration was not changed by this LAN deployment.

## Verification

Serial HTTP requests confirmed exact repository matches for the homepage
(15,234 bytes), restored photo (101,511 bytes), and TMOG banner (167,696 bytes).
The webtop CGI returned the target's live processes and a snapshot-age header;
two immediate requests shared a frame, and a request after the cache interval
returned a different frame. The counter CGI advanced zero to one, and static
reads and a browser refresh kept the total at one. That first visit is the
deployment smoke test. Browser inspection confirmed the visitor masthead,
webtop panel, and TMOG banner below it. Both source and minified-page visitor
tests passed, including reload, new-tab, and unavailable-storage cases.

## Recovery copies

On the PDP, the original runtime is preserved at:

`/usr/src/local/webtop/backups/before-20260921/runtime.tar`

The installer also made its own dated backup. A post-installation runtime
backup, including the independent counter, was produced with `make backup`.
The PDP clock reported September 19, so automatically dated backup names
reflect that clock rather than the deployment date.

The source bundle, source-host comparisons, before/after runtime backups,
HTTP verification, and checksums are kept privately on the Mac under:

`/Users/dave/Documents/Codex/2026-09-19/files-pasted-by-the-user-the/work/deploy-192-168-1-29/`

Runtime archives and mutable counts are not committed to Git. Continue using
`make backup` and copying the result off the PDP to preserve future visits.

## Redeployment after disk-image rollback, September 21

Restored the complete current application from source commit `a32f02a` after
the machine was booted from an older image. This installation uses the shared
counter and automatic failover introduced later in the day; the independent
counter described in the original installation above is historical.

- Saved the previous website, HTTP binary, service/startup configuration,
  `/etc/dtab`, and `/etc/ttys`, then copied that archive off the PDP.
- Built all four current helper binaries on `.29` and installed the minified
  homepage, TMOG-11 panel, linked TMOG banner, photograph, health endpoint,
  and shared-counter forwarding helpers. Native build and installer succeeded.
- Retained the existing HTTP binary, which matched the earlier deployment.
  Restored `/usr/sbin/inetd -R 1000` in `/etc/rc` and the running service.
- Left the working `/etc/dtab` byte-for-byte unchanged. In particular, its
  DHV probe at `160440` remains commented out. Disabling that probe allowed
  the physical machine to complete boot; preserve this working configuration
  in the saved disk image.
- Preserved Caddy's existing SQLite visitor total. An online backup was copied
  off Caddy; no initialization, reset, or count migration was performed.
- The existing automatic mode recognized `.29` as healthy and resumed using
  it, with `.26` retained as fallback. No proxy configuration change was needed.

Verification: native compilation, successful installation, existing page and
refresh/session tests, exact HTTP matches for HTML and both displayed images,
working health/counter routes, two identical immediate native sampler runs,
and advancing public snapshots labeled `X-PDP-Node: PDP .29`. A direct HTTP
snapshot comparison initially crossed the five-second cache interval; native
back-to-back execution confirmed cache reuse. The public homepage matched the
repository and the shared total continued increasing during deployment.

The post-installation runtime archive was copied off the PDP and checked for
the expected page/assets and executable permissions. Private recovery copies,
the source bundle, counter backup, boot configuration, verification, and logs:

`/Users/dave/Documents/Codex/2026-09-19/files-pasted-by-the-user-the/work/redeploy-29-20260921/`

The PDP's own clock still showed September 19, so the runtime backup is at
`/usr/src/local/webtop/backups/Sat_Sep_19_23_19_38_EDT_2026-433/runtime.tar`.
The full saved disk image should be updated after a clean shutdown. Future
visitor totals remain on Caddy and require its separate database backup.
